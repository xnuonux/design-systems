# {{projectName}} — Design System

> {{pitch}}

{{description}}

---

## Quick start

```html
<link rel="stylesheet" href="design-system/styles.css">
```

Every token is a CSS variable: `var(--accent)`, `var(--text-body)`, `var(--surface-1)`, `var(--font-display)`.

---

## Visual foundations

**Environment.** {{ground.name}} — `{{ground.hex}}`. {{ground.description}}

**Light.** {{#hasAccent}}One source: **{{accent.name}}** (`{{accent.hex}}`). {{accent.description}}{{/hasAccent}}{{^hasAccent}}No accent color — the canvas is the color. The UI stays monochromatic.{{/hasAccent}}

**Type.** {{#if hasSeparateBody}}Three voices. **{{displayFont}}** — {{displayRole}}. **{{bodyFont}}** — {{bodyRole}}. **{{monoFont}}** — {{monoRole}}.{{else}}Two voices. **{{displayFont}}** italic for titles. **{{monoFont}}** for everything else — this is a mono-forward interface.{{/if}}

**Surfaces.** {{surfaceDescription}}

**Motion.** {{motionDescription}}

---

## Voice & tone

{{#voiceRules}}
- {{.}}
{{/voiceRules}}

---

## Source DNA

| From | What |
|---|---|
{{#derivedFrom}}
| **{{.}}** | Inherited mood, palette, and type foundations |
{{/derivedFrom}}

---

## Files

```
design-system/
  styles.css          ← link this only
  tokens/
    colors.css        ← {{ground.name}}, {{accent.name}}, {{text.name}}
    typography.css    ← {{displayFont}}, {{bodyFont}}, {{monoFont}}
    effects.css       ← glow, depth, motion
    base.css          ← reset, scrollbar, selection
  README.md           ← this file
  SKILL.md            ← AI-codeable frontmatter
```

---

*Generated by [@eternities/design-systems](https://github.com/eternities-ai/design-systems)*
